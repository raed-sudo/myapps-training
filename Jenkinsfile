pipeline{
	agent any
	stages{
		stage('BUILD'){
			steps{
				echo 'This Is the Build Steps
				sh 'cd /home/deploy '
				sh 'ls -l'
				}
			}
		stage('DEPLOY'){
				echo 'This is the deploy step'
			
				}

		}
}
